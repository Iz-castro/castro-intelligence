# Deps do Cloud Run B (castro-superadmin). Enxuto: SEM faster-whisper (modelo
# pesado, so o A transcreve audio) nem pyngrok (tunel de dev). O restante e
# mantido pra cobrir o fecho de imports do servico B com folga.
fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0
bcrypt==4.2.0
PyJWT==2.9.0
python-multipart==0.0.9
google-cloud-firestore==2.19.0
google-cloud-storage==2.18.2
firebase-admin==6.7.0
google-api-python-client==2.100.0
google-auth==2.29.0
cryptography==42.0.0
