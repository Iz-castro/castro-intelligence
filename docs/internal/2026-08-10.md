# 2026-08-10 — val-5.0.1 na clinica real, horario comercial fase 1, protocolo diario destravado

> **Atualizacao 2026-08-21:** pendente 1 concluido em parte — o `varizemed-test` esta de volta ao
> DRAFT e o `varizemed` aponta hoje pro environment `22390163-6bbc-47b5-a25a-e53eb3363d8f`
> (`core_version` val-5.0.3, trocado em 21/08). **Nao use o `6bdfaeed` desta pagina como rollback**:
> a tabela vigente esta em `docs/deploy/TROCAR_ENVIRONMENT_VAL.md`. Pendentes 2 (fase 2 do horario
> comercial) e 5 (campanha de template — scripts versionados em 21/08, revisao de seguranca+Meta
> ainda incompleta) seguem abertos; 3 e 4 sem registro posterior. O bug "Val respondeu 2x",
> encerrado provisoriamente aqui, reapareceu e teve causa confirmada pra AUDIO em 18/08
> (`2026-08-18.md`).

Segunda. Continuacao direta de 2026-08-09 (guard do handoff nao atendido).
Tres frentes fechadas no dia + descoberta de que a Val decidia horario sozinha.

## val-5.0.1 na clinica real

- PO autorizou; janela conferida antes (0 conversas mid-bot na ultima hora).
- Troca cirurgica de `environment_id` no `settings.ai` do `varizemed`:
  `6bdfaeed` (val-5.0, 31/07) -> `75028a25` (val-5.0.1, publicado 08/08).
  O `scripts/set_tenant_ai` foi evitado DE PROPOSITO: reconstroi as 13 chaves
  por argv, `--location` default e `global` (varizemed e us-central1) e o
  lgpd_notice acentuado via PowerShell arrisca mojibake.
- `core_version` corrigido pra `val-5.0.1` em write separado (estava `val-05`).
  Regra nova: quem aponta environment atualiza o core_version no MESMO write.
- Rollback de tudo: `environment_id = '6bdfaeed-6dd3-445c-b32a-4e1f85ab32dd'`.
- Pendencia criada: `varizemed-test` esta no MESMO environment publicado
  (validacao de 09/08) — o Draft ficou sem nenhum tenant. Devolver pro Draft
  (`environment_id = ''`) quando o Izael for editar; docs do dev carimbados.

## A Val decidia horario sozinha (motivacao da frente b)

Levantamento nas mensagens reais de 7 dias: texto fixo "ja encerrou por hoje
(...) proximo dia util" saiu em sabado, domingo e as 07:32 de SEGUNDA (28min
antes de abrir — lead avisado de que so falariam com ele no dia seguinte); a
saudacao anunciava "seg a sex, 8h as 18h" com sexta indo ate 17h. Tudo
hardcoded no playbook do agente. O val-5.0.1 suavizou a redacao ("no proximo
horario de atendimento" — vago, nao afirma falso), mas continua adivinhando.

## Frente b fase 1 — EM PROD (rev `castro-crm-00077-5fd`, commit `5854c2c`)

`business_hours.py` = fonte unica dos 2 bots. Detalhes no commit e na spec
`docs/CX_HORARIO_COMERCIAL_DEV_IA.md` (atualizada: status implementado +
tabela de valores exatos do retorno_previsto). Pontos-chave:
- CX: `fora_do_expediente` + `retorno_previsto` em TODO turno; agente ignora
  ate o Izael ramificar/interpolar (nunca escrever hora no texto do agente).
- Builtin hubloc: aviso hardcoded "7h as 17h" substituido pelo calculo
  (8h-17h real; lead das 7h-7h59 passou a receber aviso — PO confirmou:
  empresa abre 7h, atendimento humano 8h).
- Fail-safe: tenant sem tabela NUNCA e declarado fechado.
- Fase 2 pendente: tabela no system_settings + tela admin com calendario
  mensal clicavel e feriados por CHAVE (data apodrece na virada do ano).
- De carona: log `[SMB ECHO]` do webhook redigido (unica linha com telefone
  em claro — achado do PO num log real).

## Hints do handoff (aprendizado de 09/08 consolidado)

O val-5.0.1 reescreveu a mensagem de transferencia e NENHUM dos hints de
texto casava mais — handoff ficou dependendo so do param `handoff_request`.
Lista virou ADITIVA (2 redacoes convivem, prod e teste em environments
diferentes), commit `b4414f0`, rev `castro-crm-00076-t2r`. Contrato com o
dev de IA: mexer nas frases de transferencia EXIGE aviso.

## Fix 3a — protocolo diario que nunca fechava (+ one-off)

`_close_daily_and_send_protocol` desistia quando o protocolo nao era do dia
BR corrente — e com autoclose de 20h o fechamento quase sempre cruza a
meia-noite. Resultado: `attendances_daily` preso em "aberto" pra sempre, sem
`fechado_em`, e recibo nunca enviado NEM dentro da janela. Fix: gate de
mesmo-dia vale SO pro envio do recibo; o carimbo de fechamento roda sempre.
`sim_reception_flow` 73 -> 79 (cenario 13: protocolo de ontem fecha, recibo
nao sai, protocolo de hoje segue normal).

Interacao com o modo recepcao (pergunta do PO): thread nao atendida NAO
fecha (guard) -> protocolo aberto e a VERDADE; teto de 7d fecha registro
junto; recibo de lead nao atendido bloqueado por janela+dia em todos os
caminhos. Template enviado pelo operador CONTA como atendimento humano
(`sender_user_id` no send-template -> `last_human_outbound_at`) — o fluxo
de reabertura por template funciona como desenhado.

One-off do backlog (leitura de classificacao + escrita em batch, so
`attendances_daily`; NADA enviado a leads): fecha protocolo de dia passado
apenas se superseded, ou espelho atual com TODAS as threads fechadas, ou
contato inexistente; thread aberta = atendimento vivo = intocado. Dry-run
validado pelo PO: hubloc 4.613 fechar / 18 pular; varizemed 47/63 (os 63
incluem a fila real da Recepcao — regra de seguranca provada); test 8/2.
Numeros finais da aplicacao no log da sessao.

## Tarde: one-off aplicado + 2 bugs achados pelo PO (rev `castro-crm-00079-hlp`)

One-off do backlog APLICADO: **4.669 fechados** (hubloc 4.613+1 retardatario,
varizemed 47, test 8), 82 vivos preservados; verificacao final = 0 pendentes.
Fix do protocolo em prod na rev `castro-crm-00078-glf`.

**Bug 1 — recusa LGPD assinava "A Hub Loc agradece o seu contato!" em TODOS
os tenants** (constante compartilhada, era single-tenant). Achado pelo PO com
caso real: 2 leads da varizemed recusaram em 10/08 (contatos 84 e 85/Luciane;
85 depois aceitou e foi pra fila). Fix na ideia do PO, padrao do topbar:
despedida resolve `tenant.name` em runtime; sem nome -> neutra; SEM artigo
antes do nome (artigo tem genero). Aviso default do builtin segue
hubloc-branded com comentario de alerta (vale so enquanto hubloc for o unico
tenant builtin).

**Bug 2 — audio deixava a Val muda.** Dispatch de bot so rodava pra msg_type
"text"; a transcricao (faster-whisper) e salva DEPOIS e ficava so pro
operador. Fix: apos salvar a transcricao, o webhook roda o mesmo dispatch
guardado com o transcript (contato re-lido no ponto; STT_FALLBACK_TEXT nunca
vai pro bot). Mudanca de comportamento assumida: consentimento LGPD por
AUDIO passa a valer (palavras do proprio lead). Sem sim pro caminho
webhook->stt->bot — validacao manual pendente: audio no numero de teste
apos aceite.

Commit `7efd537`, rev `castro-crm-00079-hlp` promovida por nome, smoke ok.
Rollback: `--to-revisions castro-crm-00078-glf=100`.

## Operacao / decisoes do PO no dia

- 12 leads da Recepcao sem atendimento: Cristina assumiu o CRM as 14h pra
  responder todos (lista dispensada).
- Bug "Val respondeu 2x": encerrado provisoriamente (nao reproduziu no
  5.0.1; reabre se reaparecer). Falta so a pergunta ao Izael (se o 5.0.1
  mexeu na saudacao por causa disso).
- Perfil "Recepcao" criado pelo PO na UI (herda supervisor, sem assumir).
- `.gitignore` ganhou `docs/*.csv` + `*.xlsx` (PII de campanha) — do PO.

## Pendentes ao fim do dia

1. Izael: ramificar o agente (spec pronta); antes, devolver varizemed-test
   pro Draft (1 comando, quando ele pedir).
2. Fase 2 da frente b (system_settings + calendario de feriados na UI).
3. Teste forcado no varizemed-test (metade positiva do guard de 09/08).
4. Faxina varizemed-test (bot_key `sac` duplicado; contas de teste ativas).
5. Campanha template (4.604 pendentes; revisao seguranca+Meta incompleta).
