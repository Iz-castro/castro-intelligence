# Handoff da sessão — 2026-06-07 → 2026-06-08

> **Atualização 2026-08-21:** handoff **histórico** — não leia a tabela "TL;DR"
> como estado atual. O bloqueio do **coexistence** (#4563039) caiu: o onboarding coex
> voltou a funcionar com o fix do Embedded Signup v4 + System User token
> ([2026-06-10-fix-embedded-signup-v4.md](2026-06-10-fix-embedded-signup-v4.md)), e a
> **Caixa Backup** está em prod com o histórico importado. A infra saiu de
> `southamerica-east1` para o projeto de Oregon em 2026-06-10
> ([2026-06-10-migracao-oregon.md](2026-06-10-migracao-oregon.md)) — então o comando
> de deploy, a revisão `00155-2qb` e os números de scaling desta página não valem
> mais (ver `CLAUDE.md` e `docs/deploy/DEPLOY_CLOUDRUN_A.md`). O fixo **3351-7604**
> acabou conectado como canal standard já no projeto novo (`docs/deploy/env.oregon.yaml`
> registra "canal id=4 = 3351-7604").

Documento pra **retomar em outra máquina**. Estado atual, bloqueios ativos,
próximos passos e referências técnicas. (Continuação dos diários
[2026-06-04.md](2026-06-04.md) e [2026-06-05.md](2026-06-05.md).)

---

## TL;DR — estado atual

| Item | Estado |
|---|---|
| **Standard de teste 7195-7758** (canal id=2) | **ATIVO**, CONNECTED na Meta, nome **APPROVED**. Reativado hoje pra **treinar as operadoras**. |
| **Comercial 3351-7604** | **NÃO conectado** — bloqueado na verificação por voz (PABX). Registrar **quarta-feira**. |
| **Coexistence** | Bloqueado por **#4563039** → permissão **`business_management` NÃO aprovada** no App Review. |
| **Caixa Backup** | Construída + **validada E2E** (rev 00151). Import real **pendente do embed** do comercial. |
| **Bot LGPD** | No ar (rev 00152). Roda **só com `bot_enabled=true`**. |
| **max-instances** | **5** (min 1). Subir pra 20 depois de validar o dedup atômico em prod. |
| **firestore.rules** | **Publicadas** (gates de `is_backup`). |
| Revisão Cloud Run atual | **castro-crm-00155-2qb** |

---

## 🚧 Bloqueios ativos (os dois que travam o avanço)

### 1. Comercial 3351-7604 — verificação por voz / PABX
- É **fixo** → verificação **só por voz** (fixo não recebe SMS). A ligação automática da Meta **não completa** pela URA/PABX da empresa.
- **Workaround:** desvio de chamada do 3351-7604 pra um **celular** durante a verificação (tira o robô da Meta de dentro da URA). Ou rotear direto pra um ramal sem URA.
- **Acesso ao número só quarta-feira.** Suporte da Meta já acionado.
- O lado do CRM está pronto: ao verificar → registrar com PIN (`POST /{phone_id}/register`), conferir `phone_routing→hubloc` + `channel_active`, depois importar o backup.

### 2. Coexistence — #4563039 / permissão `business_management`
- Erro ao conectar coex: *"missing required Graph API permissions for Cloud API companion pairing (#4563039)"*.
- **Causa confirmada:** no App Review (envio de **08/mai/2026**), `business_management` está **NOT approved** (os outros — `whatsapp_business_messaging`, `whatsapp_business_management` — Approved).
- **Por que trava o coex e não o standard:** o **companion pairing** do coex precisa gerenciar os **assets de negócio** do cliente → exige `business_management` em **Advanced Access**. Standard (Cloud API puro) não passa por isso.
- ⚠️ **CRM e config estão CORRETOS** (não é código): `META_APP_ID=1434723791183375`, `EMBEDDED_SIGNUP_CONFIG_ID` (coex) `=2785379481799560`, `EMBEDDED_SIGNUP_CONFIG_ID_STANDARD=2342621062931210`. O app está **publicado**.
- **Pendente de conferir:** o status **VIGENTE** de `business_management` em App Review → Permissions and Features (Standard vs Advanced). O feedback de 08/mai pode estar desatualizado.
- **Ação:** re-submeter `business_management` no App Review com caso de uso de **coexistence/companion pairing** + screencast (roteiro em [APP_REVIEW_SCREENCAST_SCRIPT.md](../APP_REVIEW_SCREENCAST_SCRIPT.md)). Ou resolver com o suporte da Meta (mesmo chamado da verificação). Claude se ofereceu pra redigir a justificativa + atualizar o roteiro.
- Obs.: o "já funcionou antes" provavelmente era número **no próprio portfólio** (Standard bastava); número novo como asset de cliente precisa de Advanced.

---

## ✅ O que foi feito nesta sessão (com commits e revisões)

1. **Caixa Backup** (aba admin/supervisor com histórico importado dos JSONs):
   - Marcador `is_backup` + `assigned_to_uid="__backup__"` (sentinela) isola o operador em 3 camadas (snapshot/rules/REST).
   - **Append ao vivo** (msg nova do cliente sobe a conversa no Backup, não vai pra Novos) + **graduação** (atribuir → sai do Backup + define vendedora de origem).
   - Helpers `create_backup_contact`/`upsert_backup_conversation`/`write_backup_message` em `database_firestore.py`.
   - Commits `80dfd43` (feature) + `884cf42` (fix: `normalizeConversation` copia `is_backup` — senão vazava pra Novos). Rev **00151**. **Validado E2E** com JSON de teste.
2. **Bot LGPD** (`bot_service.py` novo + `lgpd_bot.py`): gate de consentimento ANTES de coletar dados + **prova de consentimento** (campos `lgpd_consent`/`lgpd_consent_at`/`lgpd_policy_version` no contato + `audit_log` `LGPD_CONSENT_ACCEPTED`). Commit `0985a67`. Rev **00152**. (Lead-gruda-na-vendedora = ADR 0008, da sessão anterior.)
3. **Banner de billing #10 suprimido** (cosmético — Tech Provider não consulta billing; envio funciona via managed billing). Commit `90b1f9d`. Rev 00149.
4. **Fix data da bolha**: o chat exibia `created_at` (data do sync) em vez de `timestamp_wa` (data real da Meta) → mensagens do history apareciam com a data do sync. Commit `6309aba`. Rev **00153**.
5. **Fix retry de mídia no history sync**: se o asset de mídia chega ANTES do placeholder (corrida), enfileira retry em vez de descartar (zero perda). Commit `3463291`. Rev **00154**.
6. **Dedup atômico de mensagens**: `save_wa_message` agora claim atômico do wamid via `wa_message_index` + `create()` (espelha `wa_contact_index`) → acaba com mensagens duplicadas em rajada/concorrência. Commit `639de44`. Rev **00155**. `max-instances` 20→**5** (cinto-de-segurança pro teste; subir depois de validar).
7. Desativação + reativação do 7195-7758 (pra treino), limpeza de dados de teste.
8. Conferência da doc Meta (eventos de coex `history`/`smb_*` não estão na pasta — são guias de Coexistence à parte) e da importação de mídia.

---

## 📋 Próximos passos

### Quarta-feira (com acesso ao 3351-7604)
1. Resolver a verificação por voz (desvio pra celular ou ramal direto).
2. Embed standard do 3351-7604 → **avisar o Claude no PENDING+VERIFIED** → registrar com PIN.
3. Conferir `phone_routing→hubloc` + `channel_active=true`.
4. **Importar os 500 do backup real** (ver comando abaixo) — dry-run primeiro.
5. **Desativar o 7195-7758** (single standard ativo) — via API (`deactivate_channel`, não tem botão na UI pra standard).

### Coexistence (quando `business_management` for aprovado)
1. Aprovar `business_management` (App Review re-submit / suporte Meta).
2. Conectar os **2 coex** → testar **3 canais** (1 standard + 2 coex): isolamento por operador, sync de agenda/histórico com datas certas, mídia, sem duplicatas.

### Depois de validar
- Subir `max-instances` de volta pra **20** (com o dedup atômico, é seguro).
- Avaliar TTL na coleção `wa_message_index` (cresce ~1 doc/mensagem; o dedup só importa na janela de concorrência).

---

## 🔧 Referências técnicas

**Importador do backup:** `scripts/import_backup_hubloc.py`
```
# dry-run (só conta, nada grava):
./.venv/Scripts/python.exe -m scripts.import_backup_hubloc --dir "backup hubloc"
# importar de verdade:
./.venv/Scripts/python.exe -m scripts.import_backup_hubloc --dir "backup hubloc" --confirm
```
- Resolve o canal pelo "nosso número" (Phone de saída nos JSONs = 3351-7604) → **precisa do canal standard do 3351-7604 EXISTIR** (pós-embed).
- `normalize_br_phone` canoniza o wa_id (9º dígito) nos dois lados → casa com o webhook ao vivo.
- Idempotente (dedup por `backup_`+Message Id). Preserva timestamps + prefixo `*Nome Operador:*`.
- **Tamanho real do backup** (parse-only de hoje): **515 arquivos** (13 vazios), **490 contatos únicos**, **~19.261 mensagens**, **1.050 mídias** (viram placeholder), período **24/fev → 05/jun**. 501 dos 502 são do 3351-7604.
- Pasta `backup hubloc/` é **GITIGNORED** (PII) — nunca commitar os JSONs.

**Deploy:** `gcloud run deploy castro-crm --source . --region southamerica-east1 [--max-instances N] --quiet`
- Precisa `CLOUDSDK_PYTHON` apontando pro Python312 (já setado no PC atual; **conferir no PC novo**).
- Scripts: usar `.venv/Scripts/python.exe` (3.12). Firestore via `firestore_common` (prefixo `castro_crm`).

**Coexistence — lógica do sync** (pra revisar antes de testar):
- `smb_app_state_sync` → agenda do celular (contatos `from_message_event=False`, sem conversa, dono = canal).
- `history` → histórico ~180 dias (contato+conversa+mensagens, dono = canal).
- `smb_message_echoes` → mensagens enviadas pelo app (echo).
- `messages` → inbound normal (auto-atribuído ao dono do canal).
- Tudo de coex pertence ao **dono do canal** (não vai pro pool) → isolamento LGPD por operador é crítico.

**Memórias auto-salvas relacionadas** (`~/.claude/.../memory/`, LOCAIS — não vão por git):
- `project_backup_inbox_feature.md`, `project_standard_channel_migration.md`,
  `project_operator_isolation_lgpd.md`, `project_deploy_script_desatualizado.md`,
  `project_gcloud_python.md`. **No PC novo essas memórias não existem** — este handoff cobre o essencial.

**ADRs relevantes:** 0008 (lead gruda na vendedora), 0007 (isolamento channels/pending), 0002 (LGPD canal coex).
