# 2026-04-02

Data: 2026-04-02

## Resumo do dia

Hoje consolidamos o CRM no modelo atual `FastAPI + Firestore + Firebase Auth + React/Vite`, removendo o que ainda sobrava de compatibilidade legada no runtime e atualizando a documentacao para refletir esse estado real.

## O que foi feito

- fixamos o backend em `Firestore` e `Firebase Auth`
- removemos o caminho operacional de login legado
- simplificamos `auth.py` e `init_db.py` para o modelo Firebase-only
- mantivemos `database.py` como alias de `database_firestore.py`
- limpamos scripts de deploy para o modelo atual, sem trilha Cloud SQL no caminho principal
- deixamos o backend servindo apenas o build React em `frontend_dist/`
- removemos a ambiguidade de fallback para frontend legado em `static/`
- corrigimos a rota manual de transcricao para usar as constantes STT corretas
- corrigimos o webhook do Google Chat para salvar anexos com `await` correto
- corrigimos o calculo de `unread_count` do Google Chat com base nos operadores ativos

## Documentacao

- atualizamos o contexto do projeto para o estado real mais recente
- reescrevemos a arquitetura Firebase para refletir o runtime atual
- movemos a documentacao para `docs/`
- estruturamos `docs/` em:
  - `architecture/`
  - `deploy/`
  - `product/`
  - `decisions/`
  - `internal/`
- definimos `docs/internal/AI_AGENT_CONTEXT.md` como arquivo local/ignorado

## Git e organizacao

- `docs/` voltou a ser versionavel no Git
- `docs/internal/AI_AGENT_CONTEXT.md` ficou ignorado
- a documentacao continua fora do deploy por causa do `.gcloudignore`
- foi criado e enviado o commit:
  - `e7a20df` - `refactor: consolida runtime em Firestore/Firebase e organiza docs`

## Validacoes feitas

- `python -m py_compile` nos modulos principais: ok
- `npm run build` no frontend: ok
- deploy manual no Cloud Run: confirmado como funcionando

## Observacoes

- o maior ponto tecnico em aberto agora nao e mais legado, e sim maturidade de rules e a estrategia futura para chat interno
- se continuarmos com diario tecnico, faz sentido manter esses registros em `docs/internal/`
