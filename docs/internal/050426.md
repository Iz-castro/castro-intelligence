# 05/04/2026 - Rotacao WhatsApp / Coexistence

## Objetivo

Migrar a integracao do CRM para a nova conta WhatsApp Business `Castro Intelligence` em modo `coexistence`, recriar a app da Meta, religar webhook/tokens e apontar o Cloud Run para os IDs novos.

## IDs e ativos confirmados

- WABA atual: `Castro Intelligence` - `902456326109660`
- Numero atual: `+55 31 7195-7758`
- `WHATSAPP_PHONE_NUMBER_ID`: `1116041784915718`
- App Meta nova: `Castro Intelligence CRM` - `1434723791183375`
- App antiga removida: `FuncionaPF` - `1399887635244319`
- IDs antigos substituidos no Cloud Run:
  - `WHATSAPP_WABA_ID`: `2137446677042913`
  - `WHATSAPP_PHONE_NUMBER_ID`: `1073220935867355`
- WABA de teste criada pelo quickstart e que deve ser ignorada para producao: `2752449755094588`
- Callback do webhook: `https://castro-crm-286866630844.southamerica-east1.run.app/webhook`

## Alteracoes no repositorio

- `.env.example`
  - adicionadas observacoes explicando a diferenca entre `WHATSAPP_WABA_ID` e `WHATSAPP_PHONE_NUMBER_ID`
- `start.example.ps1`
  - adicionada a env var `WHATSAPP_WABA_ID`
- `docs/deploy/README.md`
  - documentado que:
    - `WHATSAPP_PHONE_NUMBER_ID` e `WHATSAPP_WABA_ID` entram como env vars no Cloud Run
    - `WHATSAPP_TOKEN`, `WHATSAPP_VERIFY_TOKEN` e `WHATSAPP_APP_SECRET` entram via Secret Manager
    - em `coexistence` nao se deve confundir `WABA_ID` com `PHONE_NUMBER_ID`
- `main.py`
  - log de startup passou a mostrar `waba_id` e `phone_id` quando disponiveis

## Alteracoes na Meta / GCP

- Criada a nova app Meta `Castro Intelligence CRM`
- Fluxo seguido na Meta:
  - caso de uso `Conectar-se com os clientes pelo WhatsApp`
  - integracao como `Provedor de Tecnologia independente`
- Webhook configurado com sucesso:
  - callback salvo
  - verify token existente no Secret Manager funcionou
  - campo `messages` assinado
  - teste do webhook passou com sucesso
- Gerado novo token de usuario do sistema para a app nova
- Coletado o `App Secret` da app nova
- Cloud Run atualizado para:
  - `WHATSAPP_WABA_ID=902456326109660`
  - `WHATSAPP_PHONE_NUMBER_ID=1116041784915718`
- Validado via Secret Manager que o `castro-crm-whatsapp-token` atual consegue consultar a WABA nova

## Validacoes executadas

- Teste manual do webhook:
  - `GET /webhook?hub.mode=subscribe&hub.verify_token=...&hub.challenge=123456`
  - retorno `200` com `123456`
- `gcloud run services describe castro-crm --region=southamerica-east1 --format=export`
  - confirmou os novos valores de `WHATSAPP_WABA_ID` e `WHATSAPP_PHONE_NUMBER_ID`
- `GET https://graph.facebook.com/v22.0/902456326109660/phone_numbers`
  - retornou o numero `+55 31 7195-7758`
- `GET https://graph.facebook.com/v22.0/1116041784915718`
  - retornou:
    - `display_phone_number: +55 31 7195-7758`
    - `verified_name: Castro Intelligence`
    - `quality_rating: GREEN`
    - `platform_type: ON_PREMISE`
    - `code_verification_status: NOT_VERIFIED`
    - `status: DISCONNECTED`
- Teste de envio pela Graph API:
  - `POST /1116041784915718/messages`
  - erro: `(#133010) Account not registered`

## Estado atual / bloqueio

O problema atual nao parece ser o numero de destino. O remetente `1116041784915718` ainda nao esta conectado/registrado para envio pela Cloud API.

Sinais atuais:

- `status = DISCONNECTED`
- `code_verification_status = NOT_VERIFIED`
- `platform_type = ON_PREMISE`
- erro de envio `133010 Account not registered`

Interpretacao pratica:

- WABA nova: OK
- token novo: OK
- webhook: OK
- IDs no Cloud Run: OK
- numero ainda nao esta pronto para enviar pela Cloud API / coexistence

## Proximos passos sugeridos

1. Confirmar se o `WHATSAPP_APP_SECRET` novo ja foi publicado em `castro-crm-whatsapp-app-secret`
2. Concluir o registro/conexao do numero `1116041784915718` na Cloud API / `coexistence`
3. Se a Meta pedir, usar o PIN da verificacao em duas etapas no endpoint de registro do numero (`POST /1116041784915718/register`)
4. Revalidar ate o numero sair de `DISCONNECTED` / `NOT_VERIFIED`
5. Repetir o teste de envio pela Graph API
6. Repetir o teste de envio pelo CRM

## Atualizacao 06/04/2026

- revalidado o Cloud Run `castro-crm`:
  - `WHATSAPP_WABA_ID=902456326109660`
  - `WHATSAPP_PHONE_NUMBER_ID=1116041784915718`
- revalidado o secret `castro-crm-whatsapp-token` no GCP:
  - token continua valido para consultar a WABA e o numero
- estado atual confirmado de novo via Graph API:
  - `status: DISCONNECTED`
  - `platform_type: ON_PREMISE`
  - `code_verification_status: NOT_VERIFIED`
- diferenca importante encontrada:
  - o token do `.env` local falha com `OAuthException code 190`
  - mensagem: `Application has been deleted`
  - interpretacao pratica: o ambiente local ficou com token preso na app removida, mas producao nao
- utilitario criado no repo para repetir essa checagem:
  - `python check_whatsapp_coexistence.py`
  - `python check_whatsapp_coexistence.py --token-source gcloud-secret --gcloud-project project-26fb9c99-8ee9-4179-aef`
  - `python check_whatsapp_coexistence.py --token-source both --gcloud-project project-26fb9c99-8ee9-4179-aef`

## Versionamento

- A pasta `internal/` nao esta no `.gitignore`
- Este arquivo `internal/050426.md` vai versionar normalmente
- Para abrir no outro computador:
  - `git add internal/050426.md`
  - `git add` nos demais arquivos alterados
  - `git commit`
  - `git push`
  - no outro computador, `git pull`
