# 2026-08-17 — filtro de qualificacao em toda sidebar + picker, "Carregar mais" na pool/bot, fix das notas apagadas

Domingo. Rafael operando. Pedido: entender como a qualificacao e guardada,
levar o dropdown do "Meus" pras outras caixas, "Carregar mais" tambem em
Recepcao e Bot (Varizemed precisa achar contato que ja teve atendimento).

## Como funciona (verificado, revisao Opus confirmou 6/8 afirmacoes, 2 corrigidas)

- `qualification` e string no doc de `wa_contacts` (nunca na conversation).
  Valores = `QUALIFICATION_OPTIONS` (config.py). Escritores server-side alem
  do `PUT /qualify`: auto-assign coex no webhook (novo->em_atendimento),
  contato manual com auto_assume, lead convertido que volta a escrever
  (webhook.py ~850), `return_contact_to_bot`. "Assumir" NAO mexe.
- Nao ha busca server-side por qualificacao (so `get_all_ratings` com
  convertido e o export CSV). Query simples usa indice automatico; com
  `orderBy last_message_at` exigiria composto que nao existe.
- O dropdown filtra CLIENT-SIDE (`filteredConversations`) sobre o que esta
  carregado; snapshot de CONTATOS e 50 mesmo pra admin (conversas 300) — o
  resto e hidratado via `GET /api/wa/contact/{id}` (extraContacts, 1x/sessao).

## Entregue (working tree, commit deste dia)

- Dropdown de qualificacao em Bot, Novos/Recepcao, Meus, Equipe
  (`QUALIFICATION_FILTER_VIEWS`); N/Q e Backup sem seletor. Match usa o
  mesmo fallback do chip (`|| "novo"`) — antes qualificacao vazia sumia de
  TODAS as opcoes.
- **Bug pre-existente de perda de dados**: o form Qualificacao/Notas era
  semeado de `contacts` (top-50 ao vivo), nao de `contactsById`. Thread com
  contato hidratado abria o form EM BRANCO e Salvar gravava `qualification=""`
  e APAGAVA as notas. Agora: seed via `contactsById` (`detailSeedRef`,
  re-semeia so se servidor mudou e o operador nao tocou), qualificacao vazia
  semeia "novo", contato hidratado e revalidado ao abrir a thread (1 GET),
  patch otimista em extraContacts apos salvar, e guarda no backend
  (`update_wa_contact_qualification` ignora qualification vazia).
- "Carregar mais": operador em Novos/Recepcao pagina a pool sem dono
  (`assigned_to_uid == ""` e `== null`, limite crescente, indice ja existia,
  rules = mesma shape do listener); admin em Bot/Novos reusa a janela global
  da Equipe. Gate da pool = saturacao POR TARGET vinda do listener
  (`poolLiveSaturated`; a soma enganava). Hidratacao de contatos em lotes de 8
  (rajada de 200 GETs contra `--workers 1` = 429). Resposta tardia apos trocar
  de caixa e descartada (`activeViewRef`).
- Picker "+" (agenda) agora tambem na Recepcao, com filtro por qualificacao
  (inclui nao_qualificado) e chip por linha.

## Decisoes de produto que ficaram com o PO

- Em Modo Recepcao, lead fechado volta pro bot (`bot_completed=False`) e a
  caixa Recepcao NAO o lista, mesmo com "Carregar mais". Caminho oficial =
  picker "+". As operadoras da Varizemed estao em perfil supervisor que nao
  assume: veem a caixa Bot (e o Carregar mais dela) SE `ver_todos_leads` ON.
  Liberar Bot pra operador comum ou Recepcao listar "ja atendidos" (emenda
  do ADR 0010) = decisao aberta.
- Follow-up de custo (nao feito): os tres `loadMore*` re-leem do doc 1 com
  limite crescente (300+600+900...); paginar com `startAfter` cortaria reads.

## Processo

- Revisao adversarial rodou em Opus (workflow de 23 agentes + 1 revisor
  avulso). Rafael pediu: fan-out SEMPRE em Opus, Fable so no loop principal
  (queima creditos da sessao). Anotado na memoria.
